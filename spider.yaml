# Generated by Spider

# Generates unit tests to verify that the assets exists in assets directory
generate_tests: false

# Use this to remove vcs noise created by the `generated` comments in dart code
no_comments: true

# Exports all the generated file as the one library
export: true

# This allows you to import all the generated references with 1 single import!
use_part_of: true

# Generates a variable that contains a list of all asset values.
use_references_list: false

# Location where all the generated references will be stored
package: resources

groups:
  - path: assets/images
    class_name: Images
    types: [.png, .jpg, .jpeg, .webp, .webm, .bmp]

  - path: assets/images/brands/
    class_name: Brands
    types: [.png, .jpg, .jpeg, .webp, .webm, .bmp]

  - path: assets/images/screenshots/desktop/
    class_name: ScreenshotsDesktop
    types: [.png, .jpg, .jpeg, .webp, .webm, .bmp]

  - path: assets/images/screenshots/phone/
    class_name: ScreenshotsPhone
    types: [.png, .jpg, .jpeg, .webp, .webm, .bmp]

  - path: assets/images/mockups/desktop/
    class_name: MockupsDesktop
    types: [.png, .jpg, .jpeg, .webp, .webm, .bmp]

  - path: assets/images/mockups/phone/
    class_name: MockupsPhone
    types: [.png, .jpg, .jpeg, .webp, .webm, .bmp]
#  - path: assets/vectors
#    class_name: Svgs
#    package: res
#    types: [ .svg ]
